# LEAN — καρνέ προπόνησης

Εφαρμογή καταγραφής προπόνησης για αθλητικό, γραμμωμένο σώμα. Full-body τρεις φορές την εβδομάδα, με αυτόματο έλεγχο διπλής προοδευτικότητας, παρακολούθηση λόγου ώμων προς μέση και υπολογισμό διατροφικών στόχων.

Στατικό site χωρίς build step και χωρίς εξαρτήσεις. Ανεβαίνει ως έχει στο GitHub Pages.

## Τι κάνει

- **Προπόνηση** — τρεις ημέρες (Α/Β/Γ) με προκαθορισμένες ασκήσεις, σετ, εύρη επαναλήψεων και χρόνους ξεκούρασης. Καταγράφεις κιλά και επαναλήψεις ανά σετ με δύο πατήματα.
- **Διπλή προοδευτικότητα** — όταν κλειδώσεις όλα τα σετ στο πάνω όριο του εύρους, η άσκηση σου προτείνει το επόμενο βάρος. Δεν χρειάζεται να το θυμάσαι.
- **Χρονόμετρο ξεκούρασης** — ξεκινά μόνο του μετά από κάθε σετ, με τον σωστό χρόνο για σύνθετη ή μονωτική άσκηση. Δονεί όταν τελειώσει.
- **Πρόοδος** — εκτιμώμενο 1RM ανά άσκηση, σωματικό βάρος με κυλιόμενο μέσο όρο, λόγος ώμων προς μέση, εβδομαδιαίος όγκος ανά μυϊκή ομάδα.
- **Διατροφή** — Mifflin-St Jeor, με στόχους για recomposition, γράμμωση ή lean bulk.
- **Οδηγός** — το σκεπτικό πίσω από κάθε επιλογή του προγράμματος.

Τα δεδομένα γράφονται στη συσκευή και συγχρονίζονται μόνα τους στο cloud. Δεν υπάρχει τίποτα να ρυθμίσεις.

## Ανέβασμα στο GitHub Pages

```bash
git init
git add .
git commit -m "Πρώτη έκδοση"
git branch -M main
git remote add origin https://github.com/USERNAME/lean-athletic.git
git push -u origin main
```

Μετά, στο repo: **Settings → Pages → Source: Deploy from a branch → Branch: `main` / `root` → Save.**

Σε ένα δύο λεπτά θα είναι ζωντανό στο `https://USERNAME.github.io/lean-athletic/`.

Το αρχείο `.nojekyll` υπάρχει ώστε το GitHub να σερβίρει τους φακέλους ως έχουν.

### Τοπική δοκιμή

Τα ES modules δεν φορτώνουν με διπλό κλικ στο `index.html`. Χρειάζεται server:

```bash
python3 -m http.server 8000
# άνοιξε http://localhost:8000
```

## Συγχρονισμός

Γίνεται μόνος του, χωρίς καμία ρύθμιση. Τα στοιχεία σύνδεσης είναι ενσωματωμένα στο `storage.js`.

Κάθε σετ, ζύγισμα ή μέτρηση γράφεται πρώτα στη συσκευή και μετά ανεβαίνει αυτόματα, με μισό δευτερόλεπτο καθυστέρηση ώστε να μη φορτώνεται το δίκτυο σε κάθε πάτημα. Στο άνοιγμα της εφαρμογής, όταν γυρίσεις στην καρτέλα και όταν ξαναβρεθεί σύνδεση, γίνεται πλήρης γύρος: κατέβασμα, ανακάτεμα, ανέβασμα.

Χωρίς δίκτυο όλα δουλεύουν κανονικά. Ό,τι έμεινε πίσω ξαναδοκιμάζει κάθε δύο λεπτά.

Η τελεία δίπλα στην κεφαλίδα δείχνει την κατάσταση: πράσινη συγχρονισμένο, κίτρινη σε εξέλιξη, κόκκινη χωρίς σύνδεση. Στις **Ρυθμίσεις** υπάρχει αναλυτική ένδειξη και κουμπί χειροκίνητου συγχρονισμού.

### Σύγκρουση εκδόσεων

Κάθε γραμμή κρατάει `updated_at`. Όταν η ίδια εγγραφή υπάρχει και στις δύο πλευρές, κερδίζει η πιο πρόσφατη. Η σφραγίδα ανανεώνεται μόνο όταν αλλάζει πραγματικά το περιεχόμενο, οπότε ένα απλό άνοιγμα της εφαρμογής δεν «φρεσκάρει» ψεύτικα παλιά δεδομένα.

### Πίνακες

Ζουν στο project `Barber Booking` (ref `sdddhzrbqkfayodyjnnz`) με πρόθεμα `lean_`, ώστε να μην ακουμπούν τους πίνακες του barber app που μοιράζονται την ίδια βάση.

```
lean_sessions       προπονήσεις, με τα σετ σε jsonb
lean_bodyweight     ζυγίσματα
lean_measurements   ώμοι και μέση
lean_profile        στοιχεία διατροφής, μία γραμμή
```

Η «Διαγραφή όλων» στις Ρυθμίσεις σβήνει και τις τέσσερις πλευρές — και τη συσκευή και το cloud. Αλλιώς ο επόμενος συγχρονισμός θα τα κατέβαζε πάλι.

### Σημείωση ασφάλειας

Οι πίνακες `lean_*` έχουν RLS ενεργό αλλά με πολιτική που επιτρέπει ανάγνωση και εγγραφή σε οποιονδήποτε έχει το key. Επειδή το key ταξιδεύει μέσα στη σελίδα, **όποιος ανοίξει τα developer tools στο site σου μπορεί να δει το ημερολόγιο προπόνησής σου**.

Για προσωπικό καρνέ είναι αποδεκτό. Αν σε ενοχλεί, χρειάζεται Supabase Auth και πολιτικές δεμένες με `user_id` — η στήλη υπάρχει ήδη σε όλους τους πίνακες και περιμένει.

Οι πίνακες του barber app **δεν επηρεάστηκαν** — κρατούν τις δικές τους πολιτικές.

## Δομή

```
index.html              κέλυφος και οι πέντε οθόνες
assets/css/styles.css   παλέτα, τυπογραφία, διάταξη
assets/js/data.js       πρόγραμμα, στόχοι όγκου, κείμενο οδηγού
assets/js/storage.js    localStorage με εφεδρεία μνήμης, αυτόματος συγχρονισμός
assets/js/charts.js     γραφήματα SVG χωρίς εξαρτήσεις
assets/js/app.js        καταγραφή, προοδευτικότητα, χρονόμετρο, υπολογισμοί
supabase/schema.sql     πίνακες, δείκτες, RLS (για καθαρό project από το μηδέν)
```

## Προσαρμογή

Το πρόγραμμα ζει ολόκληρο στο `PROGRAM` μέσα στο `data.js`. Για να αλλάξεις άσκηση, σετ ή εύρος επαναλήψεων, πείραξε μόνο εκείνο το αντικείμενο — η υπόλοιπη εφαρμογή προσαρμόζεται.

Το `step` ελέγχει το βήμα αύξησης βάρους: 2.5 kg για σύνθετες, 1 kg για μονωτικές. Αν το γυμναστήριό σου έχει μικρότερα πλακίδια, χαμήλωσέ το.

## Σχεδιαστικές επιλογές

Η παλέτα βγαίνει από τα χρώματα των πιστοποιημένων δίσκων: μπλε των 20 κιλών για τις ενέργειες, κίτρινο των 15 για τη στιγμή που κερδίζεις βάρος, πράσινο για ολοκληρωμένο. Η τυπογραφία είναι Roboto Condensed για τα νούμερα και IBM Plex Sans για τα υπόλοιπα, επειδή η εφαρμογή διαβάζεται όρθιος και με το ένα χέρι. Όλοι οι στόχοι αφής είναι τουλάχιστον 46 pixel.

## Άδεια

MIT. Δεν είναι ιατρική συμβουλή.
